# Xem trạng thái
git status

# Thêm files mới hoặc đã chỉnh sửa
git add .

# Commit thay đổi
git commit -m "Mô tả thay đổi"

# Push lên GitHub
git push

# Pull code mới từ GitHub
git pull

# Xem lịch sử commit
git log --oneline